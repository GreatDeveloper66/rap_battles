<%= render partial: "layouts/menu" %>
<div class="ui grid">
  <div class="three wide column">
  </div>
  <div class="ten wide column">
    <h1>All of the Venues available for Rap Battles</h1>
  </div>
  <div class="three wide column">
  </div>

<% @venues.each do |v| %>
<div class="four wide column">
  <div class="ui card">
    <div class="content">
      <p><%= v.name %>: <%= v.town.name %></p>
    </div>
    <div class="image">
      <%=image_tag("#{v.imgurl}") %>
    </div>
    <!-- <div class="content">
      <p><%= link_to "Edit this venue", edit_venue_path(v) %></p>
    </div> -->
  </div>
</div>
<% end %>


<%= link_to "Back to Users", users_path %>
</div>
